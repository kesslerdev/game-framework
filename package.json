{
  "name": "quarkit",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build:docs": "node docs.build.js",
    "clean": "lerna clean --yes && rimraf node_modules",
    "bootstrap": "yarn && lerna bootstrap",
    "build": "lerna run build",
    "demo": "npm run build && cd example && yarn start",
    "publish": "lerna publish",
    "test": "lerna run test",
    "cover": "lerna run cover",
    "cover-upload": "codecov",
    "codacy-cover-upload": "find ./packages/*/coverage -name 'lcov.info' -exec bash -c 'cat {} | ./node_modules/.bin/codacy-coverage' \\;"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kesslerdev/quarkit.git"
  },
  "author": "kesslerdev",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/kesslerdev/quarkit/issues"
  },
  "homepage": "https://github.com/kesslerdev/quarkit#readme",
  "devDependencies": {
    "@types/jest": "^19.2.3",
    "@types/node": "^7.0.22",
    "codacy-coverage": "^2.0.2",
    "lerna": "^2.0.0-rc.5",
    "rimraf": "^2.6.1",
    "tslint-config-skimia": "^1.0.1"
  }
}
